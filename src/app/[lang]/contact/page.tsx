import { Metadata } from 'next';
// components
import Wrapper from './Wrapper';

export const metadata: Metadata = {
  title: 'Contact | BMAWebDev - Your friendly neighbourhood software developer',
};

export default Wrapper;
