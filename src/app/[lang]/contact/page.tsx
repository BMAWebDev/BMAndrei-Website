import { Metadata } from 'next';
// components
import Wrapper from './Wrapper';

export const metadata: Metadata = {
  title: 'Contact | BMAWebDev - Your friendly neighbourhood developer',
};

export default Wrapper;
